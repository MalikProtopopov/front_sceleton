# Краткая сводка плана реализации управления локалями

## Цель
Переход от управления локалями внутри форм к отдельным API endpoints для независимого управления каждой локалью.

## Основные изменения

### Новые endpoints
- `POST /admin/{resource}/{id}/locales` - создать локаль
- `PATCH /admin/{resource}/{id}/locales/{locale_id}` - обновить локаль  
- `DELETE /admin/{resource}/{id}/locales/{locale_id}` - удалить локаль

### Поддерживаемые сущности (9)
**Company:** Services, Employees, Practice Areas, Advantages, Addresses  
**Content:** Topics, Articles, FAQ, Cases

## Этапы работы

### 1. Базовая инфраструктура ⚙️
- Общие типы для локалей (`src/shared/types/locale.ts`)
- Конфигурация API endpoints
- Компонент `LocaleManager` (переиспользуемый)
- Хук `useLocaleManagement`

### 2. API клиенты 🔌
Добавить методы `createLocale`, `updateLocale`, `deleteLocale` в API клиенты всех 9 сущностей

### 3. Обновление типов 📝
- Убрать обязательность `locales` из `CreateDto`
- Добавить `UpdateLocaleDto` типы

### 4. Обновление форм 📋
- Формы создания: убрать обязательные локали
- Формы редактирования: заменить управление локалями на компонент `LocaleManager`

### 5. React Query хуки 🔄
Добавить мутации для операций с локалями

### 6. Обработка ошибок ⚠️
- Утилиты для обработки специфичных ошибок локалей
- Интеграция в компоненты

### 7. Доступные локали 🌐
Хук для получения списка доступных локалей из настроек tenant

### 8. Тестирование ✅
Проверка всех операций для каждой сущности

## Приоритеты

**MVP (высокий):** Services, Articles, Cases, FAQ  
**Средний:** Topics, Employees  
**Низкий:** Practice Areas, Advantages, Addresses

## Оценка времени
24-36 часов работы

## Ключевые файлы

### Новые (7 файлов)
- `src/shared/types/locale.ts`
- `src/shared/lib/localeErrors.ts`
- `src/shared/hooks/useLocaleManagement.ts`
- `src/shared/hooks/useAvailableLocales.ts`
- `src/shared/ui/LocaleManager/LocaleManager.tsx`
- `src/shared/ui/LocaleManager/LocaleForm.tsx`
- `src/shared/ui/LocaleManager/index.ts`

### Изменяемые (~45 файлов)
- Конфигурация API endpoints
- 9 API клиентов
- 9 файлов типов
- 9 форм
- 9 React Query хуков
- 9 страниц редактирования

## Важные правила

1. ✅ Минимум одна локаль - нельзя удалить последнюю
2. ✅ Уникальность языка - один язык = одна локаль
3. ✅ Уникальность slug - в пределах locale и tenant
4. ✅ Все операции требуют permission `{resource}:update`

## Обработка ошибок

- **409** - Локаль уже существует → предложить редактировать существующую
- **400** - Минимум локалей → заблокировать удаление последней
- **409** - Slug уже существует → показать ошибку валидации
- **404** - Локаль не найдена → обновить список

## Следующие шаги

1. Начать с Этапа 1 (базовая инфраструктура)
2. Реализовать для Services как пример
3. Распространить на остальные сущности
4. Тестирование и доработка

