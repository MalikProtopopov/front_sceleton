# =============================================================================
# Admin Panel Environment Variables
# =============================================================================
# Copy this file to .env.local for local development
# For production, set these in your deployment environment (Docker, CI/CD, etc.)

# API URL - Where the backend is running
# Development: http://localhost:8000
# Production: https://api.mediann.de
NEXT_PUBLIC_API_URL=https://api.domain.com

# Admin URL - Where this admin panel is hosted (used for CORS, redirects)
NEXT_PUBLIC_ADMIN_URL=https://admin.domain.com

# Backend URL for server-side rewrites (development only)
# This is used by next.config.ts to proxy /api/* requests to backend
BACKEND_URL=http://localhost:8000
